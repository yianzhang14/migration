# lodes data

This data was sourced directly from the (lehd census website)[https://lehd.ces.census.gov/data/lodes/LODES7]. The get_wacs_data.ipynb notebook scrapes the raw data from the census website, and the agg* notebooks aggregate the resulting tract-level data to PUMA and MIGPUMA geographies. 