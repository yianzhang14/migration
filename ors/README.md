# Openrouteservice

Used to create the distance and time matrices between PUMAs/MIGPUMAs. pums_distances.ipynb in data/ relies on a local instance of openrouteservice. To run, execute `docker compose up -d` and target the container towards a [shape file for the US](https://download.geofabrik.de/north-america.html).
